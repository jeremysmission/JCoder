# Sprint Plan: R18-R22 (2026-03-19)
**Context:** R12-R17 complete, 2799 tests passing (0 failures), 67 FTS5 indexes (~14 GB).
**Focus:** Dual-GPU activation, self-ingestion, embedding pipeline repair, knowledge expansion.

---

## R18: Embedding Pipeline Repair + Self-Ingestion
**Priority:** CRITICAL -- `jcoder ingest` fails on embedding step, blocking all new FAISS indexes
**Goal:** Fix embedding wiring so JCoder can ingest codebases and build vector indexes

### Tasks
- R18.1: Debug embedding failure in `main.py ingest` -- embedding fails at chunk offset 0
  - Check EmbeddingEngine config loading and Ollama endpoint wiring
  - Verify nomic-embed-text responds to /api/embed (confirmed working via curl)
  - Fix model name mismatch: config had "nomic-embed-code" which doesn't exist
  - Already fixed: models.yaml and memory.yaml now reference nomic-embed-text
- R18.2: Self-ingest JCoder codebase (`jcoder ingest . --index-name jcoder_self`)
  - 1652 files, ~6818 chunks to embed
  - Build both FTS5 and FAISS indexes
- R18.3: Re-ingest sibling projects (HybridRAG3, LimitlessApp, CareerMoves, TheBeast)
- R18.4: Fix D: drive path references in remaining scripts
  - `scripts/overnight_download.py` -- DONE (defaults to data/ now)
  - `scripts/build_fts5_indexes.py` -- DONE (defaults to data/ now)
  - Audit remaining scripts for D: drive hardcodes
- R18.5: Tests for embedding pipeline (target: 10+)

### Exit Criteria
- `jcoder ingest . --index-name jcoder_self` succeeds end-to-end
- New FAISS index created with correct dimension (768)
- Doctor check passes with 0 warnings

---

## R19: Beast Dual-GPU 70B Bring-Up
**Priority:** HIGH -- Jeremy wants the biggest JCoder model experiments to land on Beast without changing work-stack standards
**Goal:** Import and validate one Beast-only 70B Ollama model across both RTX 3090s while keeping work and HybridRAG3 boundaries intact

### Tasks
- R19.1: Lock the Beast-only boundary
  - Keep 70B weights out of JCoder and other repo paths
  - Keep work GPU standards and the HybridRAG3 Python stack unchanged
  - Treat Beast as the largest-model test lane for JCoder
- R19.2: Import a 70B Ollama model into Beast's global model store
  - Confirm the active model path resolves outside repo directories
  - Record the chosen model name, quant, and disk footprint
- R19.3: Validate dual-GPU engagement during 70B load and prompt execution
  - Capture `nvidia-smi` plus `ollama ps` evidence during model load and one real prompt
  - Confirm both RTX 3090s hold memory and or sustained utilization
  - Record current reality: NVLink is inactive, so 70B runs must assume software sharding across two separate GPUs
- R19.4: Update operator visibility
  - Surface both GPUs and current placement expectations in docs or doctor output
  - Record rollback steps for Beast-only launch and environment changes
- R19.5: Benchmark first-response latency and steady-state behavior for the chosen 70B
- R19.6: Tests and proof pack (target: 5+)
  - Add smoke coverage or scripted validation where repo-local checks make sense
  - Save telemetry and notes as the operator proof surface

### Exit Criteria
- A 70B model imports into Beast outside repo storage
- Both GPUs are observed participating during one 70B inference run
- JCoder docs capture the Beast-only boundary, current non-NVLink assumption, and rollback path

---

## R20: Knowledge Expansion Sprint
**Priority:** HIGH -- JCoder has 67 indexes but many lack FAISS (vector) counterparts
**Goal:** Build FAISS indexes for top-priority FTS5 databases, run weekly scraper

### Tasks
- R20.1: Identify top-20 FTS5 indexes missing FAISS counterparts
  - Current FAISS indexes: ~15 (agent_memory, canary_test, career_moves, etc.)
  - Missing: coderforge (3 GB), github_code_2025 (1.6 GB), stackoverflow (880 MB), etc.
- R20.2: Build FAISS indexes for top-priority code datasets
  - Priority: code_search_net, commitpackft_python, python_docs, rfc_docs
  - Use batch embedding with nomic-embed-text
- R20.3: Run weekly scraper on schedule
  - Fresh knowledge from Python blog, Real Python, PEP, PyPI, Stack Exchange
  - Already tested: 11 new chunks ingested this session
- R20.4: Ingest fresh datasets from download queue
  - Check `config/download_queue.json` for pending items
  - Run `scripts/run_download_queue.py` for new data
- R20.5: Run distillation pilot
  - `python scripts/distill_weak_topics.py --top 20 --budget 2.0`
  - Generate expert explanations from GPT-5 for weak categories

### Exit Criteria
- Top-10 FTS5 indexes have FAISS counterparts
- Weekly scraper ran successfully with fresh chunks
- Distillation pilot produced at least 10 training pairs

---

## R21: Retrieval Eval Baseline
**Priority:** MEDIUM -- no measured quality since R12 improvements
**Goal:** Establish measurable Recall@5, MRR@10, symbol hit rate

### Tasks
- R21.1: Run `scripts/run_retrieval_eval.py` against current indexes
  - 40 golden questions from evaluation set
  - Measure: Recall@5, MRR@10, symbol hit rate
  - Save baseline results to `logs/eval_baselines/`
- R21.2: Compare FTS5-only vs FAISS-only vs hybrid retrieval
- R21.3: Test with devstral-small-2 for end-to-end answer quality
- R21.4: Document baseline in sprint status

### Exit Criteria
- Baseline numbers documented
- Clear comparison of retrieval modes

---

## R22: Self-Learning Pipeline Activation
**Priority:** MEDIUM -- pipeline exists but hasn't run end-to-end
**Goal:** Run the 6-phase learning cycle and measure improvement

### Tasks
- R22.1: Run baseline eval (Phase 1)
  - `python scripts/learning_cycle.py --eval-set evaluation/agent_eval_set_200.json --phases 1`
- R22.2: Generate study queries (Phase 2)
- R22.3: Run study engine (Phase 3)
- R22.4: Distill weak topics (Phase 4)
- R22.5: Re-eval (Phase 5)
- R22.6: Compare and report (Phase 6)

### Exit Criteria
- All 6 phases complete
- Score delta documented
- Experience replay has new entries

---

## Execution Order
R18 (fix embedding) -> R19 (dual GPU) -> R20 (expand knowledge) -> R21 (measure) -> R22 (learn)

## Known Issues Found This Session
1. `nomic-embed-code` doesn't exist as Ollama model -- config pointed to phantom model
2. `federated_search.data_dir` had literal `${JCODER_DATA}` -- env var not expanded
3. `overnight_download.py` created D: dirs at import time -- blocked test collection
4. Beast 70B validation is still unproven -- dual-GPU evidence, storage boundary, and current non-NVLink assumptions need to be captured explicitly
5. 3 test files fail collection due to D: drive mkdir (test_build_se_indexes, test_download_*)

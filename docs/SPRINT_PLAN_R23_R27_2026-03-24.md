# Sprint Plan: R23-R27 (2026-03-24)

Status note (2026-04-08):
- This file is retained as the historical R23-R27 queue.
- The current operator-facing sprint queue is now `docs/SPRINT_PLAN_R28_R32_2026-04-08.md`.
- The companion status board is `docs/SPRINT_STATUS_2026-04-08.md`.

**Context:** R18-R22 are the current work queue, but the repo and current machine state now support a tighter execution order for the demo path. Beast has dual RTX 3090s, `phi4:14b-q4_K_M` is the active reasoning model, and JCoder embedding/stress paths have been validated on live corpora.

## R23: Dual-GPU Beast Validation
**Priority:** P0
**Goal:** Capture a clean, reproducible proof pack for the Beast lane and keep it isolated from the work stack.

### Tasks
- R23.1: Record GPU inventory and idle headroom with `nvidia-smi`
- R23.2: Verify `phi4:14b-q4_K_M` and `nomic-embed-text` availability in Ollama
- R23.3: Document Beast-only boundaries and rollback steps in handoff docs
- R23.4: Capture one real model run and one embed run with timestamps
- R23.5: Save the evidence bundle for crash recovery and demo readiness

### Exit Criteria
- Dual-GPU Beast state is documented and reproducible
- The active model and embedding lane are proven on the current hardware

## R24: Ingest Saturation Pass
**Priority:** P0
**Goal:** Drive the ingest lane hard against the largest safe local corpora and measure throughput.

### Tasks
- R24.1: Run batch phase-2 embedding on the largest metadata-backed corpus
- R24.2: Prefer `coderforge` or `all_sources` for sustained throughput
- R24.3: Confirm index rebuilds complete without corruption
- R24.4: Record chunks/sec, wall time, and output artifact sizes

### Exit Criteria
- At least one large corpus completes end-to-end embed + FAISS rebuild
- Throughput and artifact sizes are recorded

## R25: Retrieval Baseline and Regression Check
**Priority:** P1
**Goal:** Establish a measurable retrieval baseline before the next round of quality work.

### Tasks
- R25.1: Run `scripts/run_retrieval_eval.py`
- R25.2: Compare FTS5-only, FAISS-only, and hybrid behavior
- R25.3: Save baseline numbers to the war room notes

### Exit Criteria
- Recall@5, MRR@10, and symbol hit rate are documented

## R26: Self-Learning Cycle
**Priority:** P1
**Goal:** Execute the learning pipeline end-to-end on the current model and data.

### Tasks
- R26.1: Run baseline eval
- R26.2: Generate study queries
- R26.3: Run study/digest/distill phases
- R26.4: Re-evaluate and compare

### Exit Criteria
- The learning cycle completes and produces a measurable delta

## R27: Demo Hardening
**Priority:** P1
**Goal:** Prepare the demo path for the least-surprise operator experience.

### Tasks
- R27.1: Reduce startup friction in docs and launchers
- R27.2: Stabilize any remaining tempdir or test-environment issues
- R27.3: Verify the emergency handoff path still matches current state
- R27.4: Tighten the smoke-test sequence for the demo flow

### Exit Criteria
- Demo launch path is repeatable and documented

## Execution Order
R23 -> R24 -> R25 -> R26 -> R27
